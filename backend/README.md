# PrepVault Backend

Node.js + Express backend for PrepVault.

## Environment

Add these keys in `backend/.env`:

- `PORT=5000`
- `MONGODB_URI=...`
- `JWT_SECRET=...`
- `CLIENT_URL=http://localhost:5173`
- `NODE_ENV=development`
  No AI-specific environment variables are required.
